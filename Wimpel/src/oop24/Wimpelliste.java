package oop24;

public class Wimpelliste {

    private int[] wimpel;
    private int maxAnzahl;
    private int wimpelanzahl;
    private char wimpelfarbe;

    //construct the wimpelliste
    public Wimpelliste(Wimpel[] wimpel) {

        for (int i = 0 ; i < wimpel() ; i++) {
        }
    }

    // get Wimpel with max occurence, if none return 0
    public int get  () {
        for(Node : Wimpel) {
            if() {
               return this.maxAnzahl;
            }
            if () {
                if wimpelkette empty add most often ocurring element of wimpel
                // else use 
                // check quality for each wimpel to add, add wimpel with optimal quality
                //
                //quality is distance to last ocurrence of wimpel, and number if ocurrences of wimpel in wimpelkette
                // check number of occurence of wimpel in kette and the distance 
                // then check for wimpel to sort 
                // if quality for two values is the same recursively sort the list
                //
                // last node in linked list is same as most often ocurrence of wimppel compare next,
            }
            else return maxAnzahl = 0;
        }
    }

    // returns 
    int getNodeAnzahl(Wimpel wimpel) {
        return wimpelAnzahl;
    }

    // returns the quality of a given colored wimpel
    int[] public get quality(Wimpel wimpel) {
        int[] quality;
        return quality;
    }

    void removeNode(Wimpel wimpel){
    }

}
